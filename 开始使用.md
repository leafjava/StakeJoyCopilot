# 🚀 开始使用 AI Copilot

## ✅ 配置状态

- ✅ OpenAI API Key 已配置
- ✅ 模型：gpt-4o-mini
- ✅ 项目依赖已安装
- ✅ 配置文件已保护（.gitignore）

## 📋 启动步骤

### 方式一：使用两个终端（推荐）

#### 终端 1：启动开发服务器
```bash
npm run dev-server
```

等待看到：
```
webpack 5.x.x compiled successfully
Server running at https://localhost:3000/
```

#### 终端 2：启动 Word 插件
```bash
npm start
```

这会自动打开 Word 并加载插件。

---

### 方式二：使用批处理文件（Windows）

1. 双击 `启动项目.bat` 启动开发服务器
2. 打开新的命令提示符，运行：
   ```bash
   npm start
   ```

---

## 🎯 测试功能

### 1️⃣ 测试文本分析

1. 在 Word 中输入一段文字：
   ```
   人工智能是计算机科学的一个分支。它研究如何让计算机模拟人类智能。
   ```

2. 选中这段文字

3. 在插件侧边栏点击 **"分析选中文本"**

4. 等待 2-5 秒，查看 AI 分析结果

**预期结果**：AI 会分析文本的结构、逻辑、用词等，并给出改进建议。

---

### 2️⃣ 测试内容生成

1. 在 "AI 生成内容" 输入框输入：
   ```
   写一段关于机器学习的简介，80字左右
   ```

2. 点击 **"生成内容"**

3. 等待 3-8 秒，查看生成结果

4. 点击 **"插入到文档"** 将内容添加到 Word

**预期结果**：AI 会根据你的需求生成相应内容。

---

### 3️⃣ 测试 Excel 分析

1. 准备一个 Excel 文件（任意 .xlsx 文件）

2. 点击 **"选择文件"** 选择文件

3. 点击 **"分析 Excel"**

4. 等待 5-10 秒，查看分析报告

**预期结果**：AI 会分析 Excel 数据并生成报告。

---

## 🔍 调试技巧

### 打开开发者工具

在 Word 中按 `F12` 或 `Ctrl + Shift + I`

### 查看日志

在开发者工具的 Console 标签中查看：
- API 调用状态
- 错误信息
- 调试日志

### 常见问题

**Q: 插件没有显示？**
- 检查开发服务器是否在运行
- 刷新 Word 或重新加载插件
- 查看控制台是否有错误

**Q: AI 响应失败？**
- 检查网络连接
- 确认 API Key 是否有效
- 查看控制台错误详情

**Q: 响应很慢？**
- 正常现象，OpenAI API 通常需要 2-10 秒
- 可以在 `src/config.js` 中改用 `gpt-3.5-turbo` 获得更快响应

---

## ⚙️ 自定义配置

### 更改 AI 模型

编辑 `src/config.js`：

```javascript
openai: {
  model: 'gpt-4',           // 最强大（慢，贵）
  // model: 'gpt-4o-mini',  // 当前配置（推荐）
  // model: 'gpt-3.5-turbo', // 最快（便宜）
}
```

### 调整响应长度

```javascript
openai: {
  maxTokens: 3000,  // 默认 2000
}
```

### 修改提示词

编辑 `src/config.js` 中的 `SYSTEM_PROMPTS`：

```javascript
export const SYSTEM_PROMPTS = {
  analyze: '你是一个专业的文本分析助手...',
  generate: '你是一个专业的内容创作助手...',
  excelAnalysis: '你是一个数据分析专家...'
};
```

---

## 🎨 自定义界面

### 修改样式

编辑 `src/taskpane/taskpane.css`：

```css
/* 修改主题色 */
.ms-Button--primary {
  background-color: #0078d4; /* 改为你喜欢的颜色 */
}

/* 调整字体大小 */
body {
  font-size: 14px;
}
```

### 修改界面文字

编辑 `src/taskpane/taskpane.html`：

```html
<h3 class="ms-font-l">📝 你的自定义标题</h3>
```

---

## 📦 添加更多功能

### 示例：添加翻译功能

1. 在 `taskpane.html` 中添加 UI：
```html
<div class="feature-section">
  <h3>🌐 翻译</h3>
  <textarea id="translateInput" placeholder="输入要翻译的文字..."></textarea>
  <button id="translateBtn" class="ms-Button ms-Button--primary">
    <span class="ms-Button-label">翻译成英文</span>
  </button>
</div>
```

2. 在 `taskpane.js` 中添加函数：
```javascript
async function translateText() {
  const text = document.getElementById("translateInput").value;
  const prompt = `请将以下文字翻译成英文：\n\n${text}`;
  const result = await aiService.generateContent(prompt);
  showResult(result);
}

// 绑定事件
document.getElementById("translateBtn").onclick = translateText;
```

---

## 🚀 部署到生产环境

### 1. 构建项目
```bash
npm run build
```

### 2. 部署文件
将 `dist/` 目录部署到支持 HTTPS 的服务器

### 3. 更新 manifest.xml
将所有 `https://localhost:3000` 替换为你的生产环境 URL

### 4. 发布插件
在 Microsoft 365 管理中心上传 manifest.xml

---

## 📚 学习资源

- [开发指南.md](./开发指南.md) - 详细的开发文档
- [项目说明.md](./项目说明.md) - 项目架构说明
- [测试说明.md](./测试说明.md) - OpenAI 配置测试
- [Office Add-ins 文档](https://learn.microsoft.com/office/dev/add-ins/)
- [Word JavaScript API](https://learn.microsoft.com/javascript/api/word)

---

## 💡 使用技巧

### 提示词优化

**文本分析**：
- "分析这段文字的专业性"
- "检查这段话的语法错误"
- "优化这段文字的表达"

**内容生成**：
- "写一段关于...的介绍，100字"
- "生成一份...的大纲"
- "扩写以下内容：..."

**Excel 分析**：
- 上传销售数据，获取趋势分析
- 上传财务报表，获取关键指标
- 上传调查问卷，获取统计分析

---

## 🎉 开始使用吧！

现在你已经准备好了：

1. ✅ 启动开发服务器
2. ✅ 打开 Word 插件
3. ✅ 测试三个核心功能
4. ✅ 根据需要自定义配置

祝你使用愉快！如有问题，请查看文档或检查控制台日志。

---

**提示**：第一次使用可能需要信任 SSL 证书，按照提示操作即可。
