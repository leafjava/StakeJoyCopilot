# AI Copilot 开发指南

## 项目已完成的调整

### ✅ 1. 从 Excel 插件改造为 Word 插件
- 修改 `manifest.xml`：Host 从 Workbook 改为 Document
- 修改 `package.json`：调试目标从 excel 改为 word
- 更新插件名称和描述

### ✅ 2. 实现核心功能
- **读取选中文本**：`getSelectedText()` 函数
- **写入内容到文档**：`writeContentToWord()` 函数
- **文本分析**：`analyzeSelectedText()` 函数
- **内容生成**：`generateContent()` 函数
- **Excel 分析**：`analyzeExcel()` 函数

### ✅ 3. 创建现代化 UI
- 重新设计侧边栏界面（`taskpane.html`）
- 优化样式（`taskpane.css`）
- 添加状态提示和结果展示区域

### ✅ 4. AI 服务架构
- 创建 `aiService.js` 封装 AI 调用逻辑
- 支持多种 AI 提供商（OpenAI、Azure、自定义后端）
- 提供模拟数据用于开发测试

### ✅ 5. 配置管理
- 创建 `config.example.js` 配置文件模板
- 支持灵活的 AI 服务配置
- 添加 `.gitignore` 保护敏感信息

### ✅ 6. 后端服务示例
- 创建 Node.js 后端服务示例（`backend-example/`）
- 实现三个核心 API 端点
- 提供完整的部署文档

### ✅ 7. 文档完善
- `README.md`：详细的项目文档
- `QUICKSTART.md`：快速开始指南
- `项目说明.md`：中文项目说明
- `开发指南.md`：本文档

## 项目结构

```
ai-copilot/
├── src/
│   ├── taskpane/
│   │   ├── taskpane.html          # 侧边栏 UI
│   │   ├── taskpane.js            # 核心逻辑
│   │   └── taskpane.css           # 样式文件
│   ├── services/
│   │   └── aiService.js           # AI 服务封装
│   ├── commands/
│   │   ├── commands.html
│   │   └── commands.js
│   └── config.example.js          # 配置模板
├── backend-example/               # 后端服务示例
│   ├── server.js
│   ├── package.json
│   ├── .env.example
│   └── README.md
├── assets/                        # 图标资源
├── manifest.xml                   # 插件配置
├── package.json
├── webpack.config.js
├── README.md                      # 英文文档
├── QUICKSTART.md                  # 快速开始
├── 项目说明.md                    # 中文说明
└── 开发指南.md                    # 本文档
```

## 使用流程

### 第一步：安装和启动

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器（终端 1）
npm run dev-server

# 3. 启动 Word 插件（终端 2）
npm start
```

### 第二步：测试功能

1. **测试文本分析**
   - 在 Word 中输入一些文字
   - 选中文字
   - 点击"分析选中文本"

2. **测试内容生成**
   - 输入需求描述
   - 点击"生成内容"
   - 点击"插入到文档"

3. **测试 Excel 分析**
   - 选择 Excel 文件
   - 点击"分析 Excel"
   - 查看分析结果

### 第三步：集成真实 AI（可选）

#### 方案 A：直接使用 OpenAI

1. 复制配置文件：
```bash
copy src\config.example.js src\config.js
```

2. 编辑 `src/config.js`：
```javascript
export const AI_CONFIG = {
  provider: 'openai',
  openai: {
    apiKey: 'sk-your-api-key',
    model: 'gpt-4'
  }
};
```

3. 在 `src/taskpane/taskpane.js` 中导入配置：
```javascript
import { AI_CONFIG } from '../config.js';
import { AIService } from '../services/aiService.js';

const aiService = new AIService(AI_CONFIG);
```

#### 方案 B：使用自建后端（推荐）

1. 启动后端服务：
```bash
cd backend-example
npm install
copy .env.example .env
# 编辑 .env 填入 API Key
npm start
```

2. 配置前端：
```javascript
export const AI_CONFIG = {
  provider: 'custom',
  custom: {
    baseURL: 'http://localhost:3001/api',
    endpoints: {
      analyze: '/analyze',
      generate: '/generate',
      excelAnalysis: '/excel-analysis'
    }
  }
};
```

## 核心代码说明

### 1. Word API 基础操作

```javascript
// 读取选中文本
async function getSelectedText() {
  return await Word.run(async (context) => {
    const selection = context.document.getSelection();
    selection.load("text");
    await context.sync();
    return selection.text;
  });
}

// 写入内容
async function writeContentToWord(text) {
  await Word.run(async (context) => {
    const body = context.document.body;
    body.insertParagraph(text, Word.InsertLocation.end);
    await context.sync();
  });
}
```

### 2. AI 服务调用

```javascript
// 使用 AI 服务
import aiService from '../services/aiService.js';

// 分析文本
const analysis = await aiService.analyzeText(selectedText);

// 生成内容
const content = await aiService.generateContent(prompt);

// 分析 Excel
const report = await aiService.analyzeExcelData(excelData);
```

### 3. UI 交互

```javascript
// 显示状态消息
function showStatus(message, duration = 3000) {
  const statusMsg = document.getElementById("statusMsg");
  statusMsg.textContent = message;
  statusMsg.classList.add("show");
  setTimeout(() => statusMsg.classList.remove("show"), duration);
}

// 显示结果
function showResult(content) {
  document.getElementById("resultContent").textContent = content;
  document.getElementById("resultSection").style.display = "block";
}
```

## 常见开发任务

### 添加新的 AI 功能

1. 在 `taskpane.html` 中添加 UI 元素
2. 在 `taskpane.js` 中添加事件处理函数
3. 在 `aiService.js` 中添加对应的 API 调用方法
4. 如果使用自建后端，在 `backend-example/server.js` 中添加新的端点

### 自定义样式

编辑 `src/taskpane/taskpane.css`：

```css
/* 修改主题色 */
.ms-Button--primary {
  background-color: #your-color;
}

/* 调整布局 */
.feature-section {
  padding: 15px;
  margin-bottom: 20px;
}
```

### 添加更多 Word 操作

```javascript
// 插入到光标位置
const range = context.document.getSelection();
range.insertText(text, Word.InsertLocation.replace);

// 设置文本格式
range.font.bold = true;
range.font.color = "blue";

// 插入表格
const table = body.insertTable(3, 2, Word.InsertLocation.end, [
  ['Header 1', 'Header 2'],
  ['Row 1 Col 1', 'Row 1 Col 2'],
  ['Row 2 Col 1', 'Row 2 Col 2']
]);
```

## 调试技巧

### 1. 查看控制台日志
在 Word 中按 `F12` 打开开发者工具

### 2. 添加调试日志
```javascript
console.log("选中的文本:", selectedText);
console.log("AI 响应:", response);
```

### 3. 错误处理
```javascript
try {
  await Word.run(async (context) => {
    // 你的代码
  });
} catch (error) {
  console.error("详细错误:", error);
  showStatus("操作失败: " + error.message);
}
```

### 4. 网络请求调试
在开发者工具的 Network 标签中查看 API 请求和响应

## 部署到生产环境

### 1. 构建前端
```bash
npm run build
```

### 2. 部署文件
将 `dist/` 目录部署到支持 HTTPS 的 Web 服务器

### 3. 更新 manifest.xml
将所有 `https://localhost:3000` 替换为生产环境 URL

### 4. 部署后端（如果使用）
```bash
cd backend-example
npm install --production
pm2 start server.js
```

### 5. 发布插件
在 Microsoft 365 管理中心上传 manifest.xml

## 性能优化建议

1. **缓存 AI 响应**：相同的请求返回缓存结果
2. **请求防抖**：避免频繁调用 API
3. **流式响应**：使用 SSE 逐步显示结果
4. **懒加载**：按需加载大型库（如 xlsx）
5. **压缩资源**：优化图片和代码体积

## 安全检查清单

- [ ] API Key 不在前端代码中
- [ ] 使用 HTTPS 通信
- [ ] 实施输入验证
- [ ] 添加请求频率限制
- [ ] 记录审计日志
- [ ] 定期更新依赖包
- [ ] 实施 CORS 策略

## 下一步计划

1. **功能增强**
   - 添加更多 AI 模型选择
   - 支持图片分析
   - 实现语音输入

2. **用户体验**
   - 添加快捷键支持
   - 实现撤销/重做功能
   - 优化加载动画

3. **企业功能**
   - 用户认证和授权
   - 团队协作功能
   - 使用统计和分析

## 获取帮助

- 查看 [README.md](./README.md)
- 查看 [QUICKSTART.md](./QUICKSTART.md)
- 查看 [项目说明.md](./项目说明.md)
- 访问 [Office Add-ins 官方文档](https://learn.microsoft.com/office/dev/add-ins/)

## 贡献指南

欢迎提交 Issue 和 Pull Request！

---

祝开发顺利！🚀
