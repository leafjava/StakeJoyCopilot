# AI Copilot - Word 智能写作助手

## 项目概述

这是一个类似 Microsoft Copilot 的 Word 原生插件（Office Add-in），使用 JavaScript、HTML、CSS 和 Office.js 库开发。插件运行在 Word 侧边栏中，可以与文档进行交互，提供 AI 驱动的智能写作辅助功能。

## 核心功能

### 1. 📝 智能文本分析
- **功能**：选中 Word 文档中的任意文字，AI 自动分析
- **分析维度**：文本结构、逻辑连贯性、用词准确性、表达专业度
- **输出**：详细的改进建议和优化方案

### 2. ✨ AI 内容生成
- **功能**：根据用户输入的需求（prompt）生成内容
- **应用场景**：
  - 文章段落生成
  - 产品描述撰写
  - 会议纪要整理
  - 报告摘要生成
- **特点**：一键插入到 Word 文档

### 3. 📄 Excel 数据分析
- **功能**：上传 Excel 文件，AI 自动分析数据
- **分析内容**：
  - 数据趋势识别
  - 关键指标提取
  - 异常数据发现
  - 专业分析报告生成
- **输出**：结构化的分析报告，可直接插入文档

## 技术架构

### 前端技术栈
- **Office.js**：微软官方提供的 Office 插件开发库
- **Word JavaScript API**：用于操作 Word 文档
- **HTML/CSS**：构建用户界面
- **Webpack**：模块打包和开发服务器

### 核心文件说明

```
项目结构：
├── manifest.xml              # 插件配置文件（定义插件名称、权限、入口）
├── src/
│   ├── taskpane/
│   │   ├── taskpane.html    # 侧边栏 UI 界面
│   │   ├── taskpane.js      # 核心业务逻辑
│   │   └── taskpane.css     # 样式文件
│   ├── services/
│   │   └── aiService.js     # AI 服务封装（支持多种 AI 提供商）
│   ├── config.example.js    # 配置文件示例
│   └── commands/            # 命令处理
├── assets/                   # 图标资源
├── package.json             # 项目依赖和脚本
└── webpack.config.js        # Webpack 配置
```

### 关键 API 实现

#### 1. 读取选中文本
```javascript
async function getSelectedText() {
  return await Word.run(async (context) => {
    const selection = context.document.getSelection();
    selection.load("text");
    await context.sync();
    return selection.text;
  });
}
```

**说明**：
- `Word.run()` 创建一个 Word API 上下文
- `getSelection()` 获取当前选中的内容
- `load("text")` 指定要加载的属性
- `context.sync()` 执行操作并同步结果

#### 2. 写入内容到文档
```javascript
async function writeContentToWord(text) {
  await Word.run(async (context) => {
    const body = context.document.body;
    body.insertParagraph(text, Word.InsertLocation.end);
    await context.sync();
  });
}
```

**说明**：
- `document.body` 获取文档正文
- `insertParagraph()` 插入段落
- `Word.InsertLocation.end` 指定插入位置（文档末尾）

## AI 服务集成

### 架构设计

项目采用服务层架构，将 AI 调用逻辑封装在 `aiService.js` 中，支持多种 AI 提供商：

1. **OpenAI**：GPT-4、GPT-3.5 等模型
2. **Azure OpenAI**：企业级 OpenAI 服务
3. **自定义后端**：自建 AI 服务（推荐用于生产环境）
4. **模拟数据**：用于开发测试

### 配置方式

#### 方式一：OpenAI
```javascript
// src/config.js
export const AI_CONFIG = {
  provider: 'openai',
  openai: {
    apiKey: 'sk-your-api-key',
    model: 'gpt-4',
    maxTokens: 2000,
    temperature: 0.7
  }
};
```

#### 方式二：Azure OpenAI
```javascript
export const AI_CONFIG = {
  provider: 'azure',
  azure: {
    endpoint: 'https://your-resource.openai.azure.com',
    apiKey: 'your-key',
    deploymentName: 'gpt-4',
    apiVersion: '2024-02-15-preview'
  }
};
```

#### 方式三：自定义后端（推荐）
```javascript
export const AI_CONFIG = {
  provider: 'custom',
  custom: {
    baseURL: 'https://your-backend.com/api',
    apiKey: 'your-api-key',
    endpoints: {
      analyze: '/analyze',
      generate: '/generate',
      excelAnalysis: '/excel-analysis'
    }
  }
};
```

### 为什么推荐自定义后端？

1. **安全性**：API Key 不会暴露在前端代码中
2. **灵活性**：可以自由切换不同的 AI 模型
3. **成本控制**：统一管理 API 调用，便于监控和限流
4. **功能扩展**：可以添加缓存、日志、审计等功能

## 开发流程

### 1. 环境准备
```bash
# 安装依赖
npm install

# 启动开发服务器（终端 1）
npm run dev-server

# 启动 Word 插件（终端 2）
npm start
```

### 2. 开发调试
- 在 Word 中按 `F12` 打开开发者工具
- 查看控制台日志和网络请求
- 修改代码后刷新插件即可看到效果

### 3. 构建部署
```bash
# 构建生产版本
npm run build

# 输出目录：dist/
```

## Excel 文件处理

### 安装 xlsx 库
```bash
npm install xlsx
```

### 使用示例
```javascript
import * as XLSX from 'xlsx';

function readExcelFile(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    
    reader.onload = (e) => {
      const data = new Uint8Array(e.target.result);
      const workbook = XLSX.read(data, { type: 'array' });
      
      // 读取第一个工作表
      const firstSheet = workbook.Sheets[workbook.SheetNames[0]];
      const jsonData = XLSX.utils.sheet_to_json(firstSheet);
      
      resolve({
        name: file.name,
        sheets: workbook.SheetNames,
        data: jsonData
      });
    };
    
    reader.readAsArrayBuffer(file);
  });
}
```

## 安全最佳实践

### 1. API Key 管理
- ❌ 不要在前端代码中硬编码 API Key
- ✅ 使用环境变量或配置文件
- ✅ 将 `src/config.js` 添加到 `.gitignore`
- ✅ 使用后端代理来调用 AI API

### 2. 输入验证
```javascript
// 验证用户输入
function validateInput(text) {
  if (!text || text.trim() === "") {
    throw new Error("输入不能为空");
  }
  
  if (text.length > 10000) {
    throw new Error("输入文本过长");
  }
  
  return text.trim();
}
```

### 3. 错误处理
```javascript
try {
  const result = await aiService.analyzeText(text);
  showResult(result);
} catch (error) {
  console.error("AI 调用失败:", error);
  showStatus("操作失败，请稍后重试");
}
```

## 常见问题

### Q1: 插件无法加载？
**A**: 检查以下几点：
1. 开发服务器是否正在运行（`npm run dev-server`）
2. manifest.xml 中的 URL 是否正确（`https://localhost:3000`）
3. 尝试清除 Office 缓存
4. 确认证书是否信任

### Q2: 无法读取选中文本？
**A**: 
1. 确保在 Word 中选中了文字
2. 检查 manifest.xml 中的权限设置（需要 `ReadWriteDocument`）
3. 查看控制台是否有错误信息

### Q3: AI 响应太慢？
**A**: 
1. 使用流式响应（Server-Sent Events）逐步显示结果
2. 添加加载动画提升用户体验
3. 考虑使用更快的 AI 模型
4. 实施请求缓存机制

### Q4: 如何部署到生产环境？
**A**: 
1. 运行 `npm run build` 构建生产版本
2. 将 `dist/` 目录部署到 Web 服务器（支持 HTTPS）
3. 更新 manifest.xml 中的 URL 为生产环境地址
4. 在 Microsoft 365 管理中心上传 manifest.xml
5. 分发给用户使用

## 扩展功能建议

### 1. 实时协作
- 多人同时编辑时的 AI 建议
- 评论和批注功能

### 2. 模板库
- 预设常用文档模板
- AI 根据模板生成内容

### 3. 多语言支持
- 自动翻译功能
- 多语言内容生成

### 4. 版本管理
- 保存 AI 生成的多个版本
- 版本对比和回滚

### 5. 数据可视化
- 将 Excel 数据转换为图表
- 自动插入到 Word 文档

## 学习资源

- [Office Add-ins 官方文档](https://learn.microsoft.com/office/dev/add-ins/)
- [Word JavaScript API 参考](https://learn.microsoft.com/javascript/api/word)
- [Office.js API 参考](https://learn.microsoft.com/javascript/api/office)
- [Office Add-ins 示例](https://github.com/OfficeDev/Office-Add-in-samples)

## 技术支持

如有问题，请查看：
1. [README.md](./README.md) - 详细文档
2. [QUICKSTART.md](./QUICKSTART.md) - 快速开始指南
3. 项目 Issues 区

## 许可证

MIT License
