# ✅ 启动前检查清单

## 配置检查

- [x] ✅ OpenAI API Key 已配置（`src/config.js`）
- [x] ✅ AI 模型设置为 gpt-4o-mini
- [x] ✅ 配置文件已添加到 .gitignore
- [x] ✅ 项目依赖已安装（node_modules 存在）
- [x] ✅ manifest.xml 已配置为 Word 插件
- [x] ✅ AI 服务已集成到 taskpane.js

## 文件检查

### 核心文件
- [x] `src/taskpane/taskpane.html` - UI 界面
- [x] `src/taskpane/taskpane.js` - 核心逻辑
- [x] `src/taskpane/taskpane.css` - 样式文件
- [x] `src/services/aiService.js` - AI 服务
- [x] `src/config.js` - OpenAI 配置（✅ 已配置）
- [x] `manifest.xml` - 插件配置

### 文档文件
- [x] `README.md` - 完整文档
- [x] `QUICKSTART.md` - 快速开始
- [x] `项目说明.md` - 中文说明
- [x] `开发指南.md` - 开发指南
- [x] `测试说明.md` - 测试说明
- [x] `开始使用.md` - 使用教程
- [x] `项目完成总结.md` - 项目总结

## 环境检查

### Node.js 和 npm
```bash
node --version  # 应该 >= 14.x
npm --version   # 应该 >= 6.x
```

### 依赖安装
```bash
# 如果还没安装，运行：
npm install
```

### 端口检查
确保端口 3000 没有被占用：
```bash
netstat -ano | findstr :3000
```

## 网络检查

- [ ] 能访问 https://api.openai.com
- [ ] 网络连接正常
- [ ] 防火墙允许 Node.js 访问网络

## 启动步骤

### 1️⃣ 启动开发服务器

打开终端 1，运行：
```bash
npm run dev-server
```

**预期输出**：
```
webpack 5.x.x compiled successfully
Server running at https://localhost:3000/
```

### 2️⃣ 启动 Word 插件

打开终端 2，运行：
```bash
npm start
```

**预期结果**：
- Word 自动打开
- 插件加载到侧边栏
- 显示 "AI Copilot" 界面

### 3️⃣ 测试功能

#### 测试 1：文本分析
1. 在 Word 中输入：`人工智能正在改变世界`
2. 选中文字
3. 点击 "分析选中文本"
4. 等待 2-5 秒
5. ✅ 应该看到 AI 分析结果

#### 测试 2：内容生成
1. 输入：`写一段关于云计算的介绍`
2. 点击 "生成内容"
3. 等待 3-8 秒
4. ✅ 应该看到生成的内容
5. 点击 "插入到文档"
6. ✅ 内容应该插入到 Word

#### 测试 3：Excel 分析
1. 准备一个 .xlsx 文件
2. 点击 "选择文件"
3. 选择文件
4. 点击 "分析 Excel"
5. 等待 5-10 秒
6. ✅ 应该看到分析报告

## 故障排查

### 问题 1：插件无法加载

**症状**：Word 打开但没有显示插件

**解决方案**：
1. 检查开发服务器是否在运行
2. 刷新 Word（关闭重开）
3. 检查 manifest.xml 中的 URL
4. 查看控制台错误（F12）

### 问题 2：AI 调用失败

**症状**：点击按钮后显示 "分析失败"

**解决方案**：
1. 按 F12 打开开发者工具
2. 查看 Console 标签的错误信息
3. 检查网络连接
4. 验证 API Key 是否正确
5. 检查 OpenAI 账户配额

**常见错误**：
```
401 Unauthorized - API Key 无效
429 Too Many Requests - 请求过多
500 Internal Server Error - OpenAI 服务问题
```

### 问题 3：证书错误

**症状**：浏览器显示证书不受信任

**解决方案**：
1. 点击 "高级"
2. 点击 "继续访问"
3. 或运行：`npx office-addin-dev-certs install`

### 问题 4：响应很慢

**症状**：AI 响应需要很长时间

**解决方案**：
1. 这是正常的，OpenAI API 通常需要 2-10 秒
2. 可以改用更快的模型：
   ```javascript
   // src/config.js
   model: 'gpt-3.5-turbo'  // 更快
   ```

## 调试技巧

### 查看日志
```javascript
// 在 taskpane.js 中添加
console.log("选中的文本:", selectedText);
console.log("AI 响应:", response);
```

### 查看网络请求
1. 按 F12 打开开发者工具
2. 切换到 Network 标签
3. 执行操作
4. 查看 API 请求和响应

### 测试 API Key
在浏览器控制台运行：
```javascript
fetch('https://api.openai.com/v1/models', {
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY'
  }
}).then(r => r.json()).then(console.log);
```

## 性能优化

### 减少响应时间
```javascript
// src/config.js
openai: {
  model: 'gpt-3.5-turbo',  // 更快
  maxTokens: 1000,         // 减少 token
}
```

### 添加缓存
可以在 aiService.js 中添加简单的缓存：
```javascript
const cache = new Map();

async function analyzeText(text) {
  if (cache.has(text)) {
    return cache.get(text);
  }
  const result = await callOpenAI(text);
  cache.set(text, result);
  return result;
}
```

## 安全检查

- [x] ✅ API Key 在 `src/config.js` 中
- [x] ✅ `src/config.js` 已添加到 .gitignore
- [x] ✅ 不会被提交到 Git
- [ ] ⚠️ 生产环境建议使用后端代理

## 准备就绪！

如果所有检查都通过，你可以：

1. ✅ 运行 `npm run dev-server`
2. ✅ 运行 `npm start`
3. ✅ 开始使用 AI Copilot！

---

## 快速命令参考

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev-server

# 启动 Word 插件
npm start

# 构建生产版本
npm run build

# 验证 manifest
npm run validate

# 停止插件
npm stop
```

---

## 获取帮助

- 📖 查看 [开始使用.md](./开始使用.md)
- 🧪 查看 [测试说明.md](./测试说明.md)
- 📚 查看 [开发指南.md](./开发指南.md)
- 🎉 查看 [项目完成总结.md](./项目完成总结.md)

---

**祝你使用愉快！** 🚀
