# StakeJoy Copilot 项目文档

## 1. 项目背景与价值

### 1.1 问题陈述

在学术写作领域，研究人员和学生面临以下核心挑战：

1. **查重率控制困难**：学术论文需要引用大量文献，导致查重率居高不下。传统的降重方法（同义词替换）往往破坏语义，降低文章质量
2. **内容扩充耗时**：论文需要达到一定字数要求，但简单的"凑字数"会影响学术价值，如何在保持质量的同时扩充内容是一大难题
3. **格式规范复杂**：不同期刊、学校、学科有不同的格式要求（APA、MLA、Chicago、GB/T 7714 等），手动调整格式费时费力且容易出错
4. **写作风格难以把握**：初学者难以掌握学术写作的规范和风格，缺乏有效的学习和模仿工具

### 1.2 市场/需求分析

- **市场规模**：
  - 全球高等教育市场规模超过 2 万亿美元
  - 中国每年毕业研究生超过 60 万人，本科生超过 800 万人
  - 学术论文发表需求持续增长，年增长率约 8-10%

- **用户痛点**：
  - **学生群体**：毕业论文查重率要求严格（通常 <15%），格式要求复杂
  - **科研人员**：投稿周期长，格式调整占用大量时间
  - **学术编辑**：处理稿件效率低，重复性工作多
  - **留学生**：英文写作能力有限，需要 AI 辅助

- **需求紧迫性**：
  - 学术诚信要求越来越高，查重系统越来越严格
  - 论文发表竞争激烈，需要提高写作效率
  - AI 技术成熟，用户对智能写作工具的接受度提高

### 1.3 解决方案概述

StakeJoy Copilot 是一个集成在 Microsoft Word 中的 AI 写作助手，提供四大核心功能：

1. **智能降重**：
   - 深度语义理解，不是简单的同义词替换
   - 保持原意的同时，重新组织句子结构
   - 支持段落级和文档级降重

2. **文本扩写**：
   - 智能分析上下文，添加相关论据和案例
   - 补充理论支撑，增加内容深度
   - 保持学术风格和逻辑连贯性

3. **论文格式化**：
   - 支持多种学术格式规范（APA、MLA、Chicago、GB/T 7714）
   - 一键应用格式，自动调整标题、引用、参考文献
   - 批量处理，提高效率

4. **范文格式仿写**：
   - 上传范文，AI 学习其结构和风格
   - 根据用户主题生成相似风格的内容
   - 帮助初学者快速掌握学术写作规范

---

## 2. 技术架构

### 2.1 技术栈

#### 前端技术
- **核心库**：Office.js（Microsoft 官方 Office 插件开发库）
- **Word API**：Word JavaScript API（操作 Word 文档）
- **开发语言**：JavaScript ES6+
- **UI 框架**：HTML5 + CSS3
- **构建工具**：Webpack 5
- **包管理**：npm

#### AI 服务
- **AI 平台**：OpenAI GPT-4、Azure OpenAI、自定义后端
- **模型选择**：
  - GPT-4：用于复杂的语义理解和内容生成
  - GPT-3.5-turbo：用于快速响应的简单任务
- **API 封装**：统一服务层，支持多种 AI 提供商切换

#### 后端服务（可选）
- **语言**：Node.js / Python
- **框架**：Express / FastAPI
- **功能**：
  - API 代理（保护 API Key）
  - 请求缓存（提高响应速度）
  - 使用统计（监控和限流）
  - 日志记录（审计和调试）

#### 部署环境
- **开发环境**：本地 Webpack Dev Server（https://localhost:3000）
- **生产环境**：任何支持 HTTPS 的 Web 服务器
- **插件分发**：Microsoft 365 管理中心 / 企业内部分发

### 2.2 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层                                │
│                   Microsoft Word                             │
│              ┌──────────────────────┐                        │
│              │   Word 文档编辑区    │                        │
│              └──────────────────────┘                        │
│              ┌──────────────────────┐                        │
│              │  插件侧边栏 (UI)     │                        │
│              │  - 智能降重          │                        │
│              │  - 文本扩写          │                        │
│              │  - 论文格式化        │                        │
│              │  - 范文仿写          │                        │
│              └──────────────────────┘                        │
└─────────────────────────┬──────────────────────────────────┘
                          │
                          │ Office.js API
                          │
┌─────────────────────────▼──────────────────────────────────┐
│                   前端应用层                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              核心业务逻辑                             │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐     │  │
│  │  │文本分析器  │  │内容生成器  │  │格式处理器  │     │  │
│  │  └────────────┘  └────────────┘  └────────────┘     │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐     │  │
│  │  │Word服务层  │  │AI服务层    │  │工具函数库  │     │  │
│  │  └────────────┘  └────────────┘  └────────────┘     │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────┬──────────────────────────────────┘
                          │
                          │ HTTPS API
                          │
┌─────────────────────────▼──────────────────────────────────┐
│                   后端服务层（可选）                         │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              API 代理服务                             │  │
│  │  - 请求转发                                           │  │
│  │  - API Key 保护                                       │  │
│  │  - 缓存管理                                           │  │
│  │  - 使用统计                                           │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────┬──────────────────────────────────┘
                          │
                          │ API 调用
                          │
┌─────────────────────────▼──────────────────────────────────┐
│                   AI 服务层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   OpenAI     │  │ Azure OpenAI │  │  自定义模型  │     │
│  │   GPT-4      │  │   GPT-4      │  │              │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 核心功能模块

#### 模块一：智能降重引擎

**功能描述**：
- 深度语义分析，理解文本含义
- 智能改写，保持原意不变
- 多层次降重策略

**技术实现**：
```javascript
// 降重核心逻辑
async function rewriteForPlagiarismReduction(text, options = {}) {
  // 1. 文本预处理
  const segments = segmentText(text);
  
  // 2. 语义分析
  const semanticInfo = await analyzeSemantics(segments);
  
  // 3. 调用 AI 改写
  const prompt = `
    请对以下学术文本进行深度改写，要求：
    1. 保持原意不变
    2. 改变句式结构
    3. 使用不同的表达方式
    4. 保持学术风格
    
    原文：${text}
  `;
  
  const rewrittenText = await aiService.generate(prompt, {
    temperature: 0.7,
    maxTokens: text.length * 2
  });
  
  // 4. 质量检查
  const quality = await checkQuality(text, rewrittenText);
  
  return {
    original: text,
    rewritten: rewrittenText,
    quality: quality
  };
}
```

**降重策略**：
1. **词汇层面**：同义词替换、词性转换
2. **句式层面**：主被动转换、句子重组
3. **段落层面**：逻辑重构、顺序调整
4. **语义层面**：深度理解后重新表达

#### 模块二：文本扩写引擎

**功能描述**：
- 智能分析上下文
- 添加论据和案例
- 补充理论支撑
- 保持逻辑连贯

**技术实现**：
```javascript
// 扩写核心逻辑
async function expandText(text, options = {}) {
  const {
    targetLength = 'double',  // 目标长度
    addExamples = true,        // 添加案例
    addTheory = true,          // 补充理论
    style = 'academic'         // 写作风格
  } = options;
  
  // 1. 分析原文
  const analysis = await analyzeText(text);
  
  // 2. 生成扩写策略
  const strategy = generateExpansionStrategy(analysis, options);
  
  // 3. 调用 AI 扩写
  const prompt = `
    请对以下文本进行学术扩写：
    
    原文：${text}
    
    扩写要求：
    - 目标长度：${targetLength}
    - 添加具体案例：${addExamples ? '是' : '否'}
    - 补充理论支撑：${addTheory ? '是' : '否'}
    - 保持学术风格和逻辑连贯性
  `;
  
  const expandedText = await aiService.generate(prompt);
  
  return expandedText;
}
```

**扩写维度**：
1. **论据扩充**：添加数据、统计、研究结果
2. **案例补充**：增加实际案例和应用场景
3. **理论深化**：补充相关理论和学术观点
4. **分析延伸**：扩展分析维度和讨论深度

#### 模块三：论文格式化引擎

**功能描述**：
- 识别文档结构
- 应用格式规范
- 批量处理
- 格式检查

**技术实现**：
```javascript
// 格式化核心逻辑
async function formatDocument(style = 'APA') {
  await Word.run(async (context) => {
    const body = context.document.body;
    
    // 1. 加载文档内容
    body.load('paragraphs');
    await context.sync();
    
    // 2. 识别文档结构
    const structure = identifyDocumentStructure(body.paragraphs);
    
    // 3. 应用格式规范
    const formatRules = getFormatRules(style);
    
    // 4. 格式化标题
    formatHeadings(structure.headings, formatRules.heading);
    
    // 5. 格式化正文
    formatBody(structure.body, formatRules.body);
    
    // 6. 格式化引用
    formatCitations(structure.citations, formatRules.citation);
    
    // 7. 格式化参考文献
    formatReferences(structure.references, formatRules.reference);
    
    await context.sync();
  });
}
```

**支持的格式规范**：
1. **APA (American Psychological Association)**
2. **MLA (Modern Language Association)**
3. **Chicago Manual of Style**
4. **GB/T 7714（中国国家标准）**
5. **IEEE**
6. **Harvard**

#### 模块四：范文仿写引擎

**功能描述**：
- 上传范文文档
- 分析写作风格
- 学习文章结构
- 生成相似内容

**技术实现**：
```javascript
// 仿写核心逻辑
async function imitateStyle(sampleDoc, userTopic, options = {}) {
  // 1. 解析范文
  const sampleContent = await parseSampleDocument(sampleDoc);
  
  // 2. 分析写作风格
  const styleAnalysis = await analyzeWritingStyle(sampleContent);
  
  // 3. 提取结构特征
  const structure = extractStructure(sampleContent);
  
  // 4. 生成仿写内容
  const prompt = `
    参考以下范文的写作风格和结构：
    
    范文分析：
    - 写作风格：${styleAnalysis.style}
    - 段落结构：${structure.paragraphPattern}
    - 论证方式：${styleAnalysis.argumentationMethod}
    - 语言特点：${styleAnalysis.languageFeatures}
    
    请根据以下主题，生成相似风格的内容：
    主题：${userTopic}
  `;
  
  const generatedContent = await aiService.generate(prompt, {
    temperature: 0.8,
    maxTokens: 3000
  });
  
  return {
    content: generatedContent,
    styleMatch: calculateStyleSimilarity(sampleContent, generatedContent)
  };
}
```

**分析维度**：
1. **结构分析**：章节划分、段落组织、逻辑顺序
2. **风格分析**：语言风格、句式特点、用词习惯
3. **论证分析**：论证方法、证据类型、推理方式
4. **格式分析**：标题格式、引用格式、排版风格

---

## 3. 开源相关信息

### 3.1 开源协议
- **协议**：MIT License
- **选择理由**：
  - 最宽松的开源协议，鼓励社区参与和商业使用
  - 允许自由修改和分发
  - 有利于项目快速传播和生态建设
  - 适合学术工具的开源分享

### 3.2 项目链接
- **在线演示**：[即将上线]
- **GitHub 仓库**：[待填写]
- **文档网站**：[待填写]
- **问题反馈**：[GitHub Issues]

### 3.3 开源状态
- [x] 已开源
- [ ] 评审后开源
- [ ] 部分模块开源

**开源范围**：
- ✅ 前端完整代码（Office.js 插件）
- ✅ AI 服务封装层
- ✅ 后端示例代码
- ✅ 部署脚本和配置
- ✅ 完整文档和教程
- ⚠️ AI API Key 需用户自行配置

---

## 4. 开发进度

### 4.1 已完成功能

- ✅ **基础架构**
  - Office.js 插件框架搭建
  - Webpack 开发环境配置
  - Word API 集成
  - 侧边栏 UI 设计

- ✅ **智能降重功能**
  - 文本选择和读取
  - AI 语义理解和改写
  - 改写结果预览
  - 一键应用到文档

- ✅ **文本扩写功能**
  - 上下文分析
  - 智能内容生成
  - 多种扩写策略
  - 质量评估

- ✅ **AI 服务集成**
  - OpenAI API 封装
  - Azure OpenAI 支持
  - 自定义后端接口
  - 错误处理和重试机制

- ✅ **文档编写**
  - 用户使用指南
  - 开发者文档
  - API 文档
  - 部署指南

### 4.2 待完善功能

- ⏳ **论文格式化**
  - APA 格式支持
  - MLA 格式支持
  - GB/T 7714 格式支持
  - 自定义格式模板

- ⏳ **范文仿写**
  - 范文上传和解析
  - 风格分析算法
  - 结构提取
  - 内容生成优化

- ⏳ **功能优化**
  - 批量处理
  - 历史记录
  - 版本对比
  - 导出报告

- ⏳ **用户体验**
  - 实时预览
  - 进度提示
  - 快捷键支持
  - 多语言界面

### 4.3 遇到的挑战与解决方案

#### 挑战 1：Word API 异步操作复杂
- **问题**：Word.run() 的异步机制容易出错，context.sync() 时机难以把握
- **解决**：
  - 封装统一的 Word 操作服务层
  - 使用 async/await 简化异步代码
  - 添加详细的错误处理和日志

#### 挑战 2：AI 响应时间长
- **问题**：GPT-4 响应时间 5-15 秒，用户体验不佳
- **解决**：
  - 添加加载动画和进度提示
  - 实现流式响应（Server-Sent Events）
  - 对简单任务使用 GPT-3.5-turbo
  - 实施请求缓存机制

#### 挑战 3：格式识别准确性
- **问题**：Word 文档格式多样，难以准确识别标题、引用等元素
- **解决**：
  - 使用 Word 内置样式（Heading 1/2/3）
  - 结合正则表达式和 AI 识别
  - 提供手动标注功能
  - 支持自定义识别规则

#### 挑战 4：API Key 安全性
- **问题**：前端代码中的 API Key 容易泄露
- **解决**：
  - 推荐使用后端代理
  - 提供后端示例代码
  - 支持环境变量配置
  - 添加使用限流和监控

### 4.4 已知问题与局限性

#### 已知问题
1. **大文档处理慢**：超过 10000 字的文档处理时间较长
2. **格式兼容性**：部分复杂格式可能无法完美识别
3. **离线不可用**：需要网络连接调用 AI API

#### 局限性
1. **依赖 AI 服务**：需要 OpenAI 或其他 AI 服务的 API Key
2. **成本考虑**：大量使用会产生 API 调用费用
3. **语言支持**：当前主要支持中英文，其他语言效果待优化
4. **学术规范**：AI 生成内容需要人工审核，确保符合学术规范

#### 改进计划
- 实现文档分块处理，提高大文档处理速度
- 引入本地模型支持，降低成本和延迟
- 添加更多语言支持
- 完善格式识别算法
- 增加离线功能（基础文本处理）

---

## 5. 技术亮点

### 5.1 深度语义理解
- 不是简单的同义词替换，而是理解语义后重新表达
- 使用 GPT-4 的强大语言理解能力
- 保持学术风格和专业性

### 5.2 原生 Word 集成
- 无缝集成到 Word 工作流程
- 无需切换软件，提高效率
- 支持 Word 桌面版和 Web 版

### 5.3 模块化架构
- 服务层分离，易于扩展和维护
- 支持多种 AI 提供商切换
- 代码结构清晰，注释详细

### 5.4 用户体验优化
- 实时预览和反馈
- 详细的错误提示
- 加载动画和进度提示
- 快捷操作和批量处理

### 5.5 安全性设计
- API Key 保护机制
- 后端代理支持
- 使用限流和监控
- 隐私数据保护

---

## 6. 未来规划

### 短期目标（1-3 个月）
- 完善论文格式化功能
- 实现范文仿写功能
- 优化 AI 响应速度
- 增加更多格式规范支持

### 中期目标（3-6 个月）
- 开发后端服务
- 实现用户系统和使用统计
- 添加协作功能
- 支持更多语言

### 长期目标（6-12 个月）
- 引入本地 AI 模型
- 开发移动端应用
- 建立模板库和社区
- 与学术平台集成

---

## 7. 团队与贡献

### 7.1 团队成员
[请在 project_info.md 中填写]

### 7.2 贡献指南
欢迎社区贡献！请查看 CONTRIBUTING.md 了解如何参与。

### 7.3 联系方式
- GitHub Issues: [仓库链接]
- 邮箱: [团队邮箱]
- 社区: [Discord/微信群]

---

## 8. 致谢

感谢以下开源项目和社区：
- Microsoft Office.js：提供强大的 Office 插件开发框架
- OpenAI：提供先进的 AI 语言模型
- Webpack：优秀的模块打包工具
- Office Add-ins Community：活跃的开发者社区
- 上海开源信息技术协会：提供比赛平台和支持
