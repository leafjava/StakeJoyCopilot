# OpenAI 配置测试说明

## ✅ 配置已完成

OpenAI API Key 已成功配置到 `src/config.js` 文件中。

### 配置详情

- **AI 提供商**: OpenAI
- **模型**: gpt-4o-mini（快速且经济）
- **API Key**: 已配置（sk-proj-HhLn5u...）
- **最大 Token**: 2000
- **Temperature**: 0.7

## 🚀 如何测试

### 1. 启动项目

打开两个终端：

**终端 1 - 启动开发服务器：**
```bash
npm run dev-server
```

**终端 2 - 启动 Word 插件：**
```bash
npm start
```

### 2. 测试功能

#### 测试 1：文本分析
1. 在 Word 文档中输入一段文字，例如：
   ```
   人工智能技术正在改变我们的生活。它可以帮助我们更高效地工作。
   ```
2. 选中这段文字
3. 在插件侧边栏点击 **"分析选中文本"**
4. 等待 AI 分析结果（通常 2-5 秒）
5. 查看分析建议

#### 测试 2：内容生成
1. 在 "AI 生成内容" 输入框中输入需求，例如：
   ```
   写一段关于云计算优势的介绍，100字左右
   ```
2. 点击 **"生成内容"** 按钮
3. 等待 AI 生成（通常 3-8 秒）
4. 查看生成的内容
5. 点击 **"插入到文档"** 将内容添加到 Word

#### 测试 3：Excel 分析
1. 准备一个简单的 Excel 文件（或使用任意 .xlsx 文件）
2. 点击 **"选择文件"** 按钮
3. 选择你的 Excel 文件
4. 点击 **"分析 Excel"** 按钮
5. 等待 AI 分析（通常 5-10 秒）
6. 查看分析报告

## 🔍 调试方法

### 查看控制台日志

1. 在 Word 中按 `F12` 打开开发者工具
2. 切换到 "Console" 标签
3. 执行操作时查看日志输出

### 常见问题排查

**问题 1：提示 "分析失败"**
- 检查网络连接是否正常
- 确认 API Key 是否有效
- 查看控制台错误信息

**问题 2：响应很慢**
- 这是正常的，OpenAI API 通常需要 2-10 秒
- 可以在 `src/config.js` 中将模型改为 `gpt-3.5-turbo` 以获得更快响应

**问题 3：API 配额错误**
- 检查 OpenAI 账户是否有足够的配额
- 访问 https://platform.openai.com/account/usage 查看使用情况

## 📊 API 调用示例

当你点击 "分析选中文本" 时，系统会发送以下请求到 OpenAI：

```json
{
  "model": "gpt-4o-mini",
  "messages": [
    {
      "role": "system",
      "content": "你是一个专业的文本分析助手。请仔细分析用户提供的文本，从结构、逻辑、用词、表达等多个维度给出专业的改进建议。请用中文回复。"
    },
    {
      "role": "user",
      "content": "用户选中的文本内容"
    }
  ],
  "max_tokens": 2000,
  "temperature": 0.7
}
```

## ⚙️ 自定义配置

### 更改 AI 模型

编辑 `src/config.js`：

```javascript
openai: {
  model: 'gpt-4',  // 更强大但更慢更贵
  // 或
  model: 'gpt-3.5-turbo',  // 更快更便宜
  // 或
  model: 'gpt-4o-mini',  // 当前配置（推荐）
}
```

### 调整响应长度

```javascript
openai: {
  maxTokens: 3000,  // 增加到 3000（更长的响应）
}
```

### 调整创意度

```javascript
openai: {
  temperature: 0.9,  // 0.0-2.0，越高越有创意
}
```

### 修改提示词

编辑 `src/config.js` 中的 `SYSTEM_PROMPTS`：

```javascript
export const SYSTEM_PROMPTS = {
  analyze: '你的自定义分析提示词...',
  generate: '你的自定义生成提示词...',
  excelAnalysis: '你的自定义 Excel 分析提示词...'
};
```

## 💰 成本估算

使用 gpt-4o-mini 模型：
- 输入：$0.150 / 1M tokens
- 输出：$0.600 / 1M tokens

典型使用场景：
- 文本分析（500 tokens）：约 $0.0003
- 内容生成（1000 tokens）：约 $0.0006
- Excel 分析（1500 tokens）：约 $0.0009

每天使用 100 次，月成本约 $1-3。

## 🔒 安全提醒

1. **不要分享 API Key**：`src/config.js` 已添加到 `.gitignore`
2. **不要提交到 Git**：确保配置文件不会被提交
3. **定期轮换密钥**：建议每月更换一次 API Key
4. **监控使用情况**：定期检查 OpenAI 使用量

## 📝 下一步

配置完成后，你可以：

1. ✅ 测试所有三个功能
2. 🎨 自定义 UI 样式（编辑 `src/taskpane/taskpane.css`）
3. 🔧 调整 AI 提示词以获得更好的结果
4. 📦 添加更多功能（如翻译、摘要等）
5. 🚀 准备部署到生产环境

## 需要帮助？

- 查看 [开发指南.md](./开发指南.md)
- 查看 [项目说明.md](./项目说明.md)
- 查看 [OpenAI API 文档](https://platform.openai.com/docs)

---

祝测试顺利！🎉
